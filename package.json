{
  "name": "academic-paper-agent",
  "version": "1.0.0",
  "description": "多 Agent 学术论文写作助手",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sql.js": "^1.10.3"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/express": "^4.17.21"
  }
}
